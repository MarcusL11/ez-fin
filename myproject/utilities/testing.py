# create a function that accepts 2 parameters, a list of strings and a string. 
# the function should return a list of strings that contain the string.

def add_to_each(numbers_list, single_number):
    return [num + single_number for num in numbers_list]
    




